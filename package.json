{
  "name": "grok-plugin-cc",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node --check plugins/grok/scripts/grok-companion.mjs && node --test tests/*.test.mjs"
  }
}
