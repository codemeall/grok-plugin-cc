---
description: Show Grok background jobs, including queued, running, completed, and failed work.
argument-hint: ""
disable-model-invocation: true
allowed-tools: Bash(grok-companion status)
---

# Grok Status

The user explicitly invoked this plugin command. Run `grok-companion status` once through Bash and present its output.

```bash
grok-companion status
```
